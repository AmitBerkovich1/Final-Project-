@using FinalProject.ViewModel
@using FinalProject.Repository
@model CreateEmployeeViewModel
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<form method="post" asp-action="Create">
    <div class="form-group">
        <label asp-for="firstName">First Name</label>
        <input asp-for="firstName" class="form-control" placeholder="Enter title">
    </div>
    <div class="form-group">
        <label asp-for="lastName">Last Name</label>
        <input asp-for="lastName" class="form-control" placeholder="Enter description">
    </div>
    <div class="form-group">
        <label asp-for="salary">Salary</label>
        <input asp-for="salary" class="form-control" placeholder="Enter Max Hours">
    </div>
    <div class="form-group">
        <select asp-for="roleId">
            <option value="">-- Select Role --</option>
            @foreach (var item in Model.roles)
            {
                <option value="@item.id">@item.title</option>
            }
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>
